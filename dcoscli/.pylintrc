[MESSAGES CONTROL]

# Disabling similarities because it complains about the `cluster setup` and
# `auth login` command definitions and I couldn't find a way to disable it at
# file level.

disable=similarities,missing-docstring

[BASIC]

# Add "e" to accepted variable names (commonly used when catching an exception)
good-names=i,j,k,ex,Run,_,e
