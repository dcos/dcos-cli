// Package config manages CLI configurations for your DC/OS clusters.
//
// Each configured cluster has an associated TOML configuration file
// which can be parsed, updated, or displayed through the Config struct.
package config
