
# This will be the actual generated bundle file that contains the plugin data
# so should not be checked in since it'll be quite large. The debug stubs will
# work for as far as allowing compilation and further testing go so are included.

bundled_plugins.gen.go
